# list - sequence of mutable values

# define a list of names
nameLists = ['blessing', 'rachel', 'buhari', 'brian']

print(nameLists[1])

# because list is mutable, i can add more names to it with built-in func
nameLists.append('rukiyah')
nameLists.sort()
print(nameLists)